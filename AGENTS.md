# Agent Guidelines

- **Build Instructions**: DO NOT run `npm run build` after editing code. Only run the build command (`npm run build`, `npm run build:frontend`, etc.) when the user explicitly requests it.
- Phản hồi bằng tiếng Việt (nhất là Implementation Plan và Walkthrough)